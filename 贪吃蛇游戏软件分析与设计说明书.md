# 贪吃蛇游戏软件分析与设计说明书

**项目名称**：贪吃蛇游戏（Snake Game V4）
**开发者**：韩志鑫
**版本**：V4.0
**日期**：2026年5月

---

## 1. 项目概述

### 1.1 背景

本项目是一个基于 Python + Pygame 开发的贪吃蛇游戏，历经四个版本迭代：

| 版本 | 阶段 | 技术栈 | 数据存储 |
|------|------|--------|---------|
| V1 | C++ 原型 | C++ + 控制台 | 无 |
| V2 | Python 重写 | Python + Pygame | txt 文件 |
| V3 | 结构化需求分析 | Python + Pygame | txt 文件 |
| V4 | 结构化设计与实现 | Python + Pygame + tkinter | SQLite3 |

### 1.2 V4 新增特性

1. **SQLite3 数据库**替代 txt 文件存储，支持用户表与游戏日志表
2. **三级难度选择**（简单 / 普通 / 困难），不同速度与加速率
3. **穿墙模式**，蛇穿越边界从对面出现
4. **金色限时食物**，+30 分，8 秒后消失，15% 概率生成
5. **音效系统**，自动生成 4 种 .wav 音效（吃食物、金色食物、游戏结束、按钮点击）
6. **UI 美化**，粒子特效、分数飘字、按钮阴影动画、颜色语义化
7. **tkinter 登录对话框**，替代 Pygame InputBox，解决中文输入法不支持问题
8. **PyInstaller 打包**，生成独立 exe 可直接双击运行

---

## 2. 需求分析

### 2.1 功能性需求

| 编号 | 需求 | 描述 |
|------|------|------|
| FR-01 | 用户注册 | 新用户输入用户名和密码完成注册，用户名不可重复 |
| FR-02 | 用户登录 | 已注册用户通过用户名和密码登录游戏 |
| FR-03 | 难度选择 | 提供简单、普通、困难三种难度，影响蛇的移动速度和加速率 |
| FR-04 | 穿墙模式 | 可选开启，蛇穿越网格边界后从对面出现 |
| FR-05 | 普通食物 | 蛇吃到食物后蛇身增长，得分 +10，速度略微加快 |
| FR-06 | 金色限时食物 | 吃普通食物后 15% 概率生成，+30 分，8 秒后自动消失，快消失时闪烁提示 |
| FR-07 | 游戏日志 | 每局游戏结束自动记录分数、时长、难度、穿墙模式等信息 |
| FR-08 | 日志查询 | 按时间倒序查看历史游戏记录 |
| FR-09 | 音效反馈 | 吃食物、金色食物、游戏结束、按钮点击各有对应音效 |
| FR-10 | 暂停/继续 | 游戏中按 ESC 或 P 暂停，再次按键继续 |

### 2.2 非功能性需求

| 编号 | 需求 | 描述 |
|------|------|------|
| NFR-01 | 中文输入支持 | 登录/注册界面必须支持中文输入法 |
| NFR-02 | 独立运行 | 打包为 exe 后无需安装 Python 环境即可运行 |
| NFR-03 | 数据兼容 | 从 txt 存储迁移到 SQLite 时，已有数据不丢失 |
| NFR-04 | 键盘焦点可靠 | tkinter 窗口关闭后，Pygame 窗口必须可靠获得键盘焦点 |

---

## 3. 系统架构

### 3.1 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 开发语言 | Python 3.12 | 主开发语言 |
| 登录界面 | tkinter | Python 内置 GUI，原生支持中文输入法 |
| 游戏引擎 | Pygame 2.6 | 2D 游戏渲染、输入处理、音效播放 |
| 数据库 | SQLite3 | Python 内置，零配置轻量级关系数据库 |
| 打包工具 | PyInstaller | 将 Python 程序打包为独立 exe |
| 音效生成 | wave + struct | Python 标准库生成正弦波 .wav 文件 |

### 3.2 两段式架构

游戏采用**两段式启动**架构：

```
┌──────────────────┐     登录成功      ┌──────────────────┐
│   阶段一：tkinter  │ ──────────────→  │   阶段二：Pygame   │
│   登录/注册对话框   │    Player 对象    │   游戏主程序       │
│   (login_dialog.py)│                  │  (snake_game.py)  │
└──────────────────┘                   └──────────────────┘
```

**设计原因**：Pygame 的 `TEXTINPUT` 事件不支持中文输入法，无法实现可用的登录/注册界面。tkinter 作为 Python 内置 GUI 库，天然支持中文输入法，且无需额外依赖。

**焦点保障**：tkinter 窗口关闭后，Windows 系统可能不会自动将焦点转移给新创建的 Pygame 窗口。通过 `ctypes.windll.user32.SetForegroundWindow(hwnd)` 强制激活 Pygame 窗口。

### 3.3 文件结构

```
Tanchishe/
├── snake_game.py                # 主程序入口（Pygame 游戏阶段）
├── login_dialog.py              # tkinter 登录/注册对话框
├── game/
│   ├── __init__.py              # 包初始化
│   ├── constants.py             # 常量配置（窗口、颜色、难度、状态等）
│   ├── database.py              # SQLite 数据库层（建表、连接、迁移）
│   ├── player.py                # 玩家类（注册/登录/统计）
│   ├── game_log.py              # 日志类（记录/查询/格式化）
│   ├── sound_manager.py         # 音效管理器（生成/播放/开关）
│   └── resources/
│       ├── msyh.ttc             # 微软雅黑字体
│       └── sounds/              # 音效文件（自动生成）
│           ├── eat.wav
│           ├── gold.wav
│           ├── gameover.wav
│           └── click.wav
├── snake_game.spec              # PyInstaller 打包配置
├── hook-rth-tkinter-fix.py     # Tcl/Tk 运行时修复钩子
├── 贪吃蛇游戏软件分析与设计说明书.md
└── README.md
```

---

## 4. 模块详细设计

### 4.1 用户管理模块

**职责**：用户注册、登录、登出、统计信息查询

**关键类**：`Player`（`game/player.py`）

**接口方法**：

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `register(username, password)` | 用户名、密码 | (bool, str) | 注册新用户，返回成功标志和提示信息 |
| `login(username, password)` | 用户名、密码 | (bool, str) | 验证登录，返回成功标志和提示信息 |
| `logout()` | — | — | 清除登录状态 |
| `get_total_games()` | — | int | 查询该玩家总游戏场次 |
| `get_high_score()` | — | int | 查询该玩家历史最高分 |
| `get_info_text()` | — | str | 获取显示用玩家信息字符串 |

**业务规则**：
- 用户名最少 3 个字符，不可重复
- 密码最少 3 个字符
- 注册时需确认密码（由 tkinter 对话框处理）
- 登录后可查询总游戏场次和最高分

**数据库表结构**：

```sql
CREATE TABLE users (
    id       INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL,
    reg_time TEXT NOT NULL
);
```

### 4.2 游戏控制模块

**职责**：游戏初始化、蛇移动控制、食物生成、碰撞检测、难度管理

**关键类**：`SnakeGame`（`snake_game.py`）

**难度配置**：

| 难度 | 初始速度(speed) | 加速率 | 最低速度 | 每食物得分 |
|------|----------------|--------|---------|-----------|
| 简单 | 10（约 6 步/秒） | 0.3 | 5 | 10 |
| 普通 | 8（约 7.5 步/秒） | 0.5 | 3 | 10 |
| 困难 | 5（约 12 步/秒） | 0.8 | 2 | 10 |

> `speed` 是帧节流阈值——游戏以 60FPS 运行，每隔 `speed` 帧移动一次蛇，值越小速度越快。

**金色食物机制**：
- 出现概率：吃普通食物后 15% 概率生成
- 存在时间：8 秒
- 得分：30 分
- 快消失时加速闪烁提示
- 到期自动消失

**穿墙模式**：
- 开启：蛇穿越网格边界时从对面出现，坐标取模 `new_x %= GRID_WIDTH`
- 关闭：碰到边界游戏结束

### 4.3 日志管理模块

**职责**：游戏日志记录、查询、格式化显示

**关键类**：`GameLog`（`game/game_log.py`）

**接口方法**：

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `save_log()` | — | — | 保存当前游戏记录到数据库 |
| `get_user_logs(username)` | 用户名 | List[dict] | 查询指定用户的日志（时间倒序） |
| `get_all_logs()` | — | List[dict] | 查询所有日志（时间倒序） |
| `format_duration(seconds)` | 秒数 | str | 格式化时长为"X分Y秒" |

**数据库表结构**：

```sql
CREATE TABLE game_logs (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id    INTEGER NOT NULL,
    username   TEXT NOT NULL,
    start_time TEXT NOT NULL,
    duration   INTEGER DEFAULT 0,
    score      INTEGER DEFAULT 0,
    difficulty TEXT DEFAULT 'normal',
    wall_mode  INTEGER DEFAULT 0,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**ER 关系**：`game_logs.user_id` → `users.id`（一对多：一个用户可有多条日志）

### 4.4 登录对话框模块

**职责**：在 Pygame 启动前完成用户认证

**关键类**：`LoginDialog`（`login_dialog.py`）

**接口方法**：

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `show()` | — | Player 或 None | 显示对话框，登录成功返回 Player，取消返回 None |

**界面状态**：
- **登录界面**：用户名 + 密码输入框，"登录" / "注册"按钮，回车键快捷登录
- **注册界面**：用户名 + 密码 + 确认密码输入框，"注册" / "返回"按钮，回车键快捷注册

**设计要点**：
- 深色主题，与游戏界面风格统一
- PyInstaller 打包时需修复 Tcl/Tk 路径（`_MEIPASS/_tcl_data`）
- 对话框关闭后，焦点需强制转给 Pygame 窗口

### 4.5 音效管理模块

**职责**：音效文件生成、加载、播放控制

**关键类**：`SoundManager`（`game/sound_manager.py`）

**音效列表**：

| 音效 | 频率 | 时长 | 特点 |
|------|------|------|------|
| eat（吃食物） | 880Hz | 0.1s | 正弦波 + 淡出 |
| gold（金色食物） | C6 + E6 | 0.2s | 双音和弦 + 淡出 |
| gameover（游戏结束） | 440→220Hz | 0.5s | 下降音调 + 淡出 |
| click（按钮点击） | 600Hz | 0.05s | 短促正弦波 |

**接口方法**：

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `init()` | — | — | 初始化 pygame.mixer，加载/生成音效 |
| `play(name)` | 音效名 | — | 播放指定音效 |
| `toggle()` | — | bool | 切换音效开关，返回当前状态 |

**懒加载策略**：音效文件不存在时自动生成，避免首次运行无声音。

### 4.6 数据库层模块

**职责**：数据库连接管理、表结构初始化、数据迁移

**关键函数**（`game/database.py`）：

| 函数 | 说明 |
|------|------|
| `get_connection()` | 获取数据库连接（启用 Row 工厂模式） |
| `init_database()` | 创建 users 和 game_logs 表（IF NOT EXISTS） |
| `migrate_from_txt()` | 从 users.txt / gamelog.txt 迁移旧数据到 SQLite |

**路径策略**：
- 源码运行：数据库在项目根目录
- exe 运行：数据库在 exe 旁（`os.path.dirname(sys.executable)`），确保可写

---

## 5. 界面状态机

### 5.1 Pygame 状态

Pygame 游戏阶段有 6 个界面状态：

```
                          ┌──────────┐
                 登录成功  │  STATE_  │
            ┌────────────→│  MENU    │
            │             └────┬─────┘
            │                  │ 开始游戏
            │                  ▼
            │             ┌──────────┐
            │             │ STATE_   │
            │             │ DIFFICULTY│
            │             └────┬─────┘
            │                  │ 选定难度
            │                  ▼
       ┌────┴────┐       ┌──────────┐
       │ 退出游戏 │←──────│ STATE_   │←──── ESC/P
       └─────────┘       │ PLAYING  │──────→┌──────────┐
                         └────┬─────┘       │ STATE_   │
                              │ 碰撞/死亡    │ PAUSED   │
                              ▼             └──────────┘
                         ┌──────────┐
                         │ STATE_   │
                         │ GAMEOVER │
                         └────┬─────┘
                              │ 返回主菜单
                              ▼
                         ┌──────────┐
                         │ STATE_   │
                         │  MENU    │
                         └──────────┘
                              │ 查看日志
                              ▼
                         ┌──────────┐
                         │ STATE_   │
                         │   LOG    │
                         └──────────┘
```

> **注意**：登录/注册不经过 Pygame 状态机，而是在游戏启动前由 tkinter 对话框完成。

### 5.2 状态说明

| 状态 | 常量 | 说明 |
|------|------|------|
| 主菜单 | STATE_MENU | 显示欢迎信息、总场次/最高分、三个操作按钮 |
| 难度选择 | STATE_DIFFICULTY | 三级难度按钮 + 穿墙模式开关 + 返回按钮 |
| 游戏中 | STATE_PLAYING | 20×20 网格 + 蛇 + 食物 + 信息面板 + 粒子特效 |
| 暂停 | STATE_PAUSED | 半透明覆盖层 + "游戏暂停"提示 |
| 游戏结束 | STATE_GAMEOVER | 得分/时长/难度/穿墙模式统计 |
| 日志 | STATE_LOG | 按时间倒序显示历史游戏记录 |

---

## 6. 核心算法

### 6.1 蛇移动帧节流

游戏以 60FPS 运行，但蛇不是每帧都移动。使用帧节流机制控制速度：

```
frame_counter += 1
if frame_counter >= speed:
    frame_counter = 0
    移动蛇()
```

- `speed` 值越大，蛇移动越慢（间隔帧数多）
- 每吃一个食物，`speed -= speed_increment`，速度加快
- `speed` 下限为 `min_speed`，防止过快无法操作

**方向缓冲**：使用 `next_direction` 缓冲方向输入，同帧内只接受最后一次方向变更，防止快速连按导致蛇反转。

**防反转**：不接受与当前方向相反的输入（如向右时不接受向左）。

### 6.2 穿墙坐标取模

```python
new_x = (snake_head_x + direction_dx) % GRID_WIDTH
new_y = (snake_head_y + direction_dy) % GRID_HEIGHT
```

关闭穿墙时，碰到边界（`new_x < 0` 或 `>= GRID_WIDTH` 等）直接判定游戏结束。

### 6.3 金色食物生命周期

```
吃普通食物 → 15% 概率生成金色食物
              │
              ├─ 记录生成时间 (gold_food_timer)
              │
              ├─ 每帧检查: 已存在时间 > 8秒 → 自动消失
              │
              ├─ 快消失时(剩余<2秒) → 闪烁加速(0.3秒/次)
              │
              └─ 蛇吃到 → +30分, 播放gold音效, 粒子爆发
```

### 6.4 音效生成算法

使用 Python `wave` + `struct` + `math` 模块生成正弦波音效：

1. 计算采样数：`n_samples = sample_rate × duration`
2. 逐采样计算振幅：`value = volume × fade × sin(2π × frequency × t)`
3. 淡出处理：后 30% 采样线性衰减
4. 写入 .wav 文件：16bit PCM，单声道，22050Hz

### 6.5 数据迁移算法

从 txt 到 SQLite 的迁移策略：

1. 调用 `init_database()` 创建表结构
2. 读取 `users.txt`，按 `|` 分隔解析，`INSERT OR IGNORE` 插入
3. 读取 `gamelog.txt`，按 `|` 分隔解析，`INSERT OR IGNORE` 插入
4. 使用 `OR IGNORE` 确保重复数据不会报错

---

## 7. 部署与打包

### 7.1 PyInstaller 打包

**配置文件**：`snake_game.spec`

**打包命令**：
```bash
pyinstaller --noconfirm snake_game.spec
```

**产物**：`dist/贪吃蛇游戏.exe`（约 45MB）

### 7.2 资源路径适配

PyInstaller 打包后 `__file__` 指向临时解压目录（`sys._MEIPASS`），需要区分只读资源和可写数据：

| 文件类型 | 源码模式路径 | exe 模式路径 | 原因 |
|---------|------------|------------|------|
| 字体（msyh.ttc） | 项目目录 | `sys._MEIPASS` | 只读，随 exe 打包 |
| 音效（.wav） | 项目目录 | exe 旁 `sounds/` | 需要可写（自动生成） |
| 数据库（.sqlite） | 项目目录 | exe 旁 | 需要可写（运行时创建） |

### 7.3 Tcl/Tk 兼容性

使用 Anaconda 环境打包时，系统自带的 Tcl DLL（8.6.5）与 Anaconda 的 Tcl（8.6.14）版本冲突。

**解决方案**：
1. 在 spec 文件中显式添加 Anaconda 的 `tcl86t.dll` / `tk86t.dll`
2. 创建运行时钩子 `hook-rth-tkinter-fix.py`，在 tkinter 导入前设置 `TCL_LIBRARY` / `TK_LIBRARY` 环境变量指向 `_tcl_data` 目录
