# 贪吃蛇游戏软件分析与设计说明书

**项目名称**：贪吃蛇游戏（Snake Game V4）  
**开发者**：韩志鑫  
**日期**：2026年5月3日  
**版本**：V4.0（实验四迭代版本）

---

## 1. 项目概述

本项目是一个基于 Python + Pygame 开发的贪吃蛇游戏，在实验三基础上进行了结构化设计与功能增强迭代。主要新增功能包括：SQLite数据库存储、三级难度选择、穿墙模式、金色限时食物、音效系统和UI美化。

## 2. 系统架构

### 2.1 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 开发语言 | Python 3.12 | 主开发语言 |
| 游戏框架 | Pygame 2.6 | 2D游戏渲染和输入处理 |
| 数据库 | SQLite3 | Python内置，轻量级关系数据库 |
| 音效 | Pygame Mixer | 自动生成.wav音效 |
| 图表 | Mermaid.js | 流程图和层次图渲染 |

### 2.2 文件结构

```
Tanchishe/
├── snake_game.py              # 主程序入口（V4）
├── snake_db.sqlite            # SQLite数据库文件
├── game/
│   ├── __init__.py            # 包初始化
│   ├── constants.py           # 常量配置（窗口、颜色、难度等）
│   ├── database.py            # SQLite数据库层
│   ├── player.py              # 玩家类（注册/登录）
│   ├── game_log.py            # 日志类（记录/查询）
│   ├── sound_manager.py       # 音效管理器
│   └── resources/
│       ├── msyh.ttc           # 微软雅黑字体
│       └── sounds/            # 音效文件（自动生成）
│           ├── eat.wav        # 吃食物音效
│           ├── gold.wav       # 金色食物音效
│           ├── gameover.wav   # 游戏结束音效
│           └── click.wav      # 按钮点击音效
├── screenshots/               # 界面截图
├── 功能模块层次方框图.html
├── 蛇移动算法流程图.html
├── 实验四WBS图.html
└── 贪吃蛇游戏软件分析与设计说明书.md
```

## 3. 功能模块设计

### 3.1 用户管理模块

**功能**：用户注册、登录、登出、玩家信息查询

**数据库设计**：

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL,
    reg_time TEXT NOT NULL
);
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 用户ID |
| username | TEXT | UNIQUE NOT NULL | 用户名 |
| password | TEXT | NOT NULL | 密码 |
| reg_time | TEXT | NOT NULL | 注册时间 |

**业务规则**：
- 用户名最少3个字符，不可重复
- 密码最少3个字符，注册时需确认密码
- 登录后可查询总游戏场次和最高分

### 3.2 游戏控制模块

**功能**：难度选择、游戏初始化、蛇移动控制、食物生成、碰撞检测、穿墙模式、金色限时食物

**难度配置**：

| 难度 | 初始速度(speed) | 加速率 | 最低速度 | 每食物得分 |
|------|----------------|--------|---------|-----------|
| 简单 | 10（约6步/秒） | 0.3 | 5 | 10 |
| 普通 | 8（约7.5步/秒） | 0.5 | 3 | 10 |
| 困难 | 5（约12步/秒） | 0.8 | 2 | 10 |

**金色食物机制**：
- 出现概率：吃普通食物后15%概率生成
- 存在时间：8秒
- 得分：30分
- 快消失时加速闪烁提示
- 到期自动消失

**穿墙模式**：
- 开启后蛇穿越网格边界时从对面出现
- 使用取模运算实现：`new_x %= GRID_WIDTH`
- 关闭时碰到边界游戏结束

### 3.3 日志管理模块

**功能**：游戏日志记录、日志查询、日志展示

**数据库设计**：

```sql
CREATE TABLE game_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    username TEXT NOT NULL,
    start_time TEXT NOT NULL,
    duration INTEGER DEFAULT 0,
    score INTEGER DEFAULT 0,
    difficulty TEXT DEFAULT 'normal',
    wall_mode INTEGER DEFAULT 0,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 日志ID |
| user_id | INTEGER | FOREIGN KEY → users(id) | 用户ID |
| username | TEXT | NOT NULL | 用户名（冗余，方便查询） |
| start_time | TEXT | NOT NULL | 游戏开始时间 |
| duration | INTEGER | DEFAULT 0 | 游戏时长（秒） |
| score | INTEGER | DEFAULT 0 | 最终得分 |
| difficulty | TEXT | DEFAULT 'normal' | 难度等级 |
| wall_mode | INTEGER | DEFAULT 0 | 穿墙模式（0=关，1=开） |

### 3.4 UI显示模块

**7个界面状态**：

1. **登录界面**（STATE_LOGIN）：用户名/密码输入框，登录/注册按钮
2. **注册界面**（STATE_REGISTER）：用户名/密码/确认密码输入框，确认/返回按钮
3. **主菜单界面**（STATE_MENU）：欢迎信息、总场次/最高分统计、开始游戏/查看日志/退出按钮
4. **难度选择界面**（STATE_DIFFICULTY）：简单/普通/困难按钮、穿墙模式开关、返回按钮
5. **游戏界面**（STATE_PLAYING）：20×20网格、蛇、食物、信息面板、粒子特效
6. **暂停界面**（STATE_PAUSED）：半透明覆盖层 + "游戏暂停"提示
7. **游戏结束界面**（STATE_GAMEOVER）：得分/时长/难度/穿墙模式统计

**UI增强**：
- 按钮悬停变色效果
- 按钮阴影
- 绿色确认按钮 / 红色危险按钮
- 粒子特效（吃食物时爆发）
- 分数飘字效果（+10/+30飘起淡出）
- 金色食物闪烁动画
- 主菜单蛇形装饰动画

## 4. 算法设计

### 4.1 蛇移动算法

采用**帧节流**机制控制蛇的移动速度：

1. 游戏以60FPS运行
2. `speed`变量控制每隔多少帧移动一次
3. `frame_counter`累计帧数，达到`speed`阈值时移动
4. 速度越快，`speed`值越小（最短间隔）

**关键优化**：
- 使用`next_direction`缓冲方向输入，防止同帧内多次改向
- 防反转逻辑：不接受与当前方向相反的输入
- 碰撞检测在移动前执行，避免蛇进入非法位置

### 4.2 数据迁移算法

从txt文件到SQLite的迁移：
1. 初始化SQLite数据库，创建表结构
2. 读取users.txt，逐行解析并插入users表
3. 读取gamelog.txt，逐行解析并插入game_logs表
4. 使用`INSERT OR IGNORE`避免重复数据

### 4.3 音效生成算法

使用Python的`wave`和`struct`模块生成正弦波音效：
- **吃食物**：880Hz，0.1秒，带淡出
- **金色食物**：C6+E6双音和弦，0.2秒
- **游戏结束**：440Hz→220Hz下降音调，0.5秒
- **按钮点击**：600Hz，0.05秒

## 5. 测试

### 5.1 功能测试项

| 测试项 | 预期结果 | 实际结果 |
|--------|---------|---------|
| 用户注册 | 新用户写入SQLite，不可重复 | ✓ 通过 |
| 用户登录 | 验证用户名密码，查询SQLite | ✓ 通过 |
| 难度选择 | 3种难度速度不同 | ✓ 通过 |
| 穿墙模式 | 蛇穿越边界从对面出现 | ✓ 通过 |
| 普通食物 | +10分，加速 | ✓ 通过 |
| 金色食物 | +30分，8秒后消失，闪烁提示 | ✓ 通过 |
| 日志记录 | 游戏结束写入SQLite | ✓ 通过 |
| 日志查询 | 按时间倒序显示 | ✓ 通过 |
| 音效播放 | 吃食物/金色/结束/点击音效 | ✓ 通过 |
| 粒子特效 | 吃食物时粒子爆发 | ✓ 通过 |

## 6. 与实验三的迭代对比

| 方面 | 实验三（V3） | 实验四（V4） |
|------|-------------|-------------|
| 数据存储 | txt文件 | SQLite3数据库 |
| 难度 | 无（固定速度） | 3级难度选择 |
| 穿墙 | 无 | 可选穿墙模式 |
| 食物 | 仅普通食物 | 普通+金色限时 |
| 音效 | 无 | 4种音效 |
| UI | 基础 | 粒子特效+飘字+闪烁动画 |
| 日志字段 | 6个 | 8个（+difficulty, wall_mode） |
| 界面状态 | 7个 | 8个（+难度选择） |
